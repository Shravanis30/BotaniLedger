// Data constants to be replaced by real API calls
// Previously contained demo data, now initialized as empty for real-world production use.

export const demoBatches = [];
export const demoProducts = [];
export const demoAlerts = [];
export const demoStats = {
  totalBatches: 0,
  activeUsers: 0,
  openAnomalies: 0,
  recentActivity: []
};
